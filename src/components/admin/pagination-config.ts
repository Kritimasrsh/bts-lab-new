/** Shared pagination constants — plain module (no "use client") so both server
 *  pages and the client Pagination component can import the real values. */
export const PER_PAGE_OPTIONS = [10, 20, 50, 100];
export const DEFAULT_PER_PAGE = 20;
